<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Group04_AINutritionPlanner">
        <activity
            android:name=".view.activity.ProfileEditActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.MainActivity" android:exported="false" />
        <activity android:name=".view.activity.MealEntryActivity" android:exported="false" />
        <activity android:name=".view.activity.NutritionActivity" android:exported="false" />
        <activity android:name=".view.activity.SettingsActivity" android:exported="false" />

        <service android:name=".service.FoodAnalysisService" android:exported="false" />
        <service android:name=".service.FirebaseSyncService" android:exported="false" />

        <receiver android:name=".receiver.MealReminderReceiver" android:exported="false" />
        <receiver android:name=".receiver.NetworkChangeReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
        <receiver android:name=".receiver.BootCompletedReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name=".data.provider.MealLogContentProvider"
            android:authorities="com.example.a23110035_23110060.provider"
            android:exported="false" />
    </application>

</manifest>
